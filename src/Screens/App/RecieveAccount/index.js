import React, { Component, useEffect, useState } from 'react';
import {
  View,
  Text,
} from 'react-native';

const ReceiveAccount = () => {
    useEffect(() => {
    }, [])

    return (
        <View style ={{backgroundColor:"white", flex:1}}>
            <Text> Recieve Account</Text>
        </View>

    );

}

export default ReceiveAccount;