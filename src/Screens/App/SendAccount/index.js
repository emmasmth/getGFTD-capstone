import React, { Component, useEffect, useState } from 'react';
import {
    View,
    Text,
} from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

const SendAccount = () => {
    useEffect(() => {
    }, [])

    return (

        <View style={{backgroundColor:"white", flex: 1 }}>
            <Text> Send Account</Text>
        </View>


    );

}

export default SendAccount;